@foreach (var shape in Enum.GetValues<AvatarShape>())
{
    <div class="flex items-center space-x-2">
        <Avatar Shape="@shape">
            <AvatarFallback>RT</AvatarFallback>
        </Avatar>
    </div>
}