@namespace ShadcnBlazor.Docs.Pages.Examples

<Accordion Collapsible Class="w-full">
    @foreach (var item in _items)
    {
        <AccordionItem @key="item.title">
            <AccordionTrigger Class="">
                <div class="flex gap-3 items-center">
                    <LucideIcon Name="@item.icon" />
                    @item.title
                </div>
            </AccordionTrigger>
            <AccordionContent>
                @item.content
            </AccordionContent>
        </AccordionItem>
    }
</Accordion>

@code {
    List<(string title, string content, IconName icon)> _items = new()
    {
        ("Is it accessible?", "Yes. It adheres to the WAI-ARIA design pattern.", IconName.Contrast),
        ("Is it styled?", "Yes. It comes with default styles that matches the other components' aesthetic.", IconName.Palette),
        ("Is it animated?", "Yes. It's animated by default, but you can disable it if you prefer.", IconName.Zap)
    };
}